node_modules/
.env
*.log

# Migration sources copied from the Flutter app's assets — large, and the data
# lives in MongoDB once loaded.
data/*.sqlite
data/*.db
data/*.db-wal
data/*.db-shm
data/*.json
